spring.application.name=scm2.0
server.port=8081

# mysql database configurations
# data source url
spring.datasource.url=jdbc:mysql://localhost:3306/scm20
spring.datasource.username=root
spring.datasource.password=Akankshya@123

#** JPA(java persistence API)API for interacting with relational databases using Java objects. helps you save, retrieve, update, and delete Java objects in a database without writing SQL for every operation.
#** Jpa configuration ::  JPA configuration means setting up your Spring Boot application so that it knows:Which database to connect to,Which database driver to use, Database username/password
#** yeh jpa queries ko chalayega jo mujhe console pe dikhega
spring.jpa.show-sql=true 
#** Imp for creating schemas and tables
spring.jpa.hibernate.ddl-auto=update
#**dialect: which query you are writing mysql or plsql , how springboot will know which database query to be make or use through dialect springboot know which database query to be written mean how jpa will automatically make msql query how it will know it will now by dialect
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
#spring.jpa.properties.hibernate.formate_sql=true